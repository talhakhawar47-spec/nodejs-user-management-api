# nodejs-user-management-api
A simple Node.js REST API built without frameworks. It supports user management (CRUD operations), file-based storage using JSON, and basic middleware authentication using custom headers.
